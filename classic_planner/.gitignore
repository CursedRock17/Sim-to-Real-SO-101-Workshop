# colcon build artifacts
build/
install/
log/

# The robot description must be tracked despite the repo-root *.urdf / *.stl
# ignore rules -- the MoveIt config is useless without them.
!so101_moveit_config/config/so101.urdf
!so101_moveit_config/meshes/
!so101_moveit_config/meshes/*.stl
